# Ranges rather than exact pins: the Gemini and LangGraph packages move fast, and
# pinning them to the day would mean a security update needs a code change. Upper
# bounds keep the next major version from arriving unannounced on a redeploy.
aiogram>=3.17,<4
langgraph>=1.0,<2
langgraph-checkpoint-postgres>=2.0.25,<3
langchain-core>=1.0,<2
langchain-google-genai>=4.3,<5
psycopg[binary,pool]>=3.2,<4
httpx>=0.27,<0.29
pydantic>=2.9,<3
pydantic-settings>=2.6,<3
APScheduler>=3.11,<4
