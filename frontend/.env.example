VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
# Backend base URL. Vite proxies /api to http://localhost:3000 in dev, so this can stay unset locally.
# VITE_API_URL=http://localhost:3000/api
# "Спросить Барса" deep link — same value as the backend's TELEGRAM_BOT_USERNAME.
# Optional: falls back to the current production bot handle if unset.
# VITE_TELEGRAM_BOT_USERNAME=bars_teenagespace_bot
